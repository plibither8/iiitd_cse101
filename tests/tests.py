# Exponentiation using recursion
def exp(x, n):
    if n is 0: return 1
    return x * exp(x, n - 1)

# Subset enumeration - find power set
def power_set(): pass

# Decimal to binary
def decToBin(dec): pass
